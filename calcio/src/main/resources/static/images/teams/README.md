# Team Logos Directory

This directory is used to drop initial team logos (e.g. `Roma.png`, `Lazio.jpg`). 
The `TeamImageSeeder` component will pick them up at application startup and save them as Base64 strings in the database.
